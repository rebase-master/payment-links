# Pinned so this repo always resolves peer dependencies the same way
# `npm ci` does in CI, regardless of a machine's global npm config (e.g. a
# legacy-peer-deps=true left over from an older project). false is npm's
# own current default — this file exists to make that non-negotiable here.
legacy-peer-deps=false
